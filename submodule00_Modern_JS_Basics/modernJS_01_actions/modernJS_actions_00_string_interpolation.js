/* How to Add and mix strings in modern JS */

// In old JS:
const lyrics = "never gonna give you up";
console.log("i'm " + lyrics);

// New method using the "backtick" "`"
// With the backtick we can use it as a single string:
console.log(`I'm ${lyrics}`);

// This will outùt the same as before
