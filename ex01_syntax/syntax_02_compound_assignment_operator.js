/*
How to assign values to vars and have those assignments perform actions.
*/

// Example data:
var name = 'Tiffany';
var sum = 0;
var gradeOne = 100;
var gradeTwo = 80;

// How to add 2 things:
	// Let's add gradeOne to sum:
	sum += gradeOne;
	console.log(sum);

// All operators work like this:
	// Let's reverse it:
	sum -= gradeOne;
	console.log(sum);

// Same with *, /, %...